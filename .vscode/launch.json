{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "python package",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/msys2_wrapper/main.py",
            "console": "integratedTerminal",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}